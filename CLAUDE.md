# AI 브리핑 — 프로젝트 가이드

한국어 AI 이슈 블로그 (Hugo + PaperMod, `F:\GlmWork\aidaily`).
**최종 목표: AdSense 광고 수익.** 모든 콘텐츠·기능 결정은 "검색 유입 → 수익" 관점에서 판단한다.

## 세션 시작할 때
1. `F:\GlmWork\PROJECT-STATE.md` 먼저 읽기 — 진행 단계·할 일·서버 상태가 거기 있다.
2. 미리보기: `cd F:\GlmWork\aidaily` 후 `hugo server -b http://localhost:1313/` → http://localhost:1313
   (`-b` 없이 띄우면 baseURL 플레이스홀더 때문에 CSS·링크가 전부 404)

## 글 작성 규칙

**선정 기준** (아래 중 2개 이상 충족):
① 최근 24~48시간 내 확인 가능한 새 사실 ② 한국 독자에게 미치는 영향 ③ 업계 지형의 구조적 변화

**형식**:
- 파일명: 영문 — URL이 `/posts/<파일명>/`으로 생성됨
- 프런트매터: `title` / `date` / `description`(150자 내) / `summary`(목록 요약) / `categories` 1개(모델·산업·정책·보안·연구) / `tags` 2~3개 / `draft: true`(검수 후 false)
- 뉴스 브리핑 템플릿: **3줄 요약 → 왜 중요한가 → 핵심 정리(표 적극 사용) → 업계 반응 → 앞으로 볼 포인트 → 출처(링크)**

**콘텐츠 유형 2가지** (병행 — 뉴스는 발행 당일 이후 유입이 죽으므로 검색형이 수익의 근간. **비율: 뉴스 30~40% / 가이드 60~70%**):
- **뉴스 브리핑** (주 2~3편 — 유입용): 위 템플릿 그대로, 선정 기준 충족 시만
- **검색형 가이드** (주 3~4편 — 자산용): 몇 주~몇 달간 검색되는 주제. 소재는 `prompts/guide-backlog.md` 우선순위 순. 규칙:
  - 제목에 검색 키워드 포함 — "서비스명 + 가격/사용법/총정리/비교" 형태
  - **롱테일 우선**: "직군×AI 프롬프트"·"작업 단위 자동화"·"툴 비교" — 신생 사이트는 "ChatGPT 사용법"급 대표 키워드로 승부하지 않는다. AI 입문류는 도메인 파워 쌓인 뒤 세그먼트 앵글(예: "50대 직장인")로
  - 상단에 **빠른 답**(핵심 질문 3개 즉답), 본문은 검색 의도별 섹션 구성
  - 비교·가격은 반드시 표로, **FAQ 3~5개** 포함
  - 글 첫머리에 "**YYYY년 M월 D일 기준**" 명시, 정보 변경 시 기존 글 업데이트 (새 글 X)
  - 관련 뉴스 글로 **내부 링크** 걸기, 태그에 `가이드` 포함
  - 프롬프트·사용법 예시는 직접 실행해 확인한 것만 — 검증 안 된 프롬프트 나열 금지

**뉴스↔가이드 플라이휠**: 대형 뉴스(신모델 출시·가격 변경 등)가 터지면 뉴스 브리핑과 함께 그 주제의 가이드(가격·사용법)를 발행해 상호 내부 링크 — 뉴스는 속도, 가이드는 지속 유입 (GPT-6 Astra 구조의 일반화)

**팩트 규칙**:
- 인용은 공식 발표·주요 언론(연합뉴스·AI타임스·TechCrunch·Reuters 등)만
- 숫자·벤치마크는 원 출처에서 직접 확인 — 확인 안 되는 사실은 쓰지 않는다
- 출처 링크 없는 글은 발행 금지
- 뉴스 사진 무단 사용 금지 — 커버는 자동 생성 SVG(`card_svg.html`)로 충분

**제휴 콘텐츠 규칙** (AdSense 승인 후 병행 개시):
- 제휴 링크가 포함된 글은 첫머리에 공개 문구 필수 (예: "이 글에는 제휴 링크가 포함되어 있습니다")
- 제휴 링크는 **검색형 가이드에만** 부착 — 뉴스 브리핑에는 금지 (신뢰도 보호)
- 제휴 상품은 실제 비교·검증 기반으로만 추천

## 기술 주의사항
- PaperMod 신규 구조: partials는 `layouts/_partials/`임 (구 `layouts/partials/` 아님). 같은 이름 파일이 테마를 오버라이드
- 커스텀 CSS: `assets/css/extended/*.css` — 지문화된 단일 stylesheet로 번들됨
- 다크모드: `:root[data-theme="dark"]` 속성 방식
- 배포: push → GitHub Actions 자동 빌드·배포 (baseURL 자동 교정되므로 로컬 플레이스홀더 그대로 둔다)

## 글 추가 요청을 받으면
`prompts/new-article.md` 절차를 따른다: 리서치 → 사실 검증 → 작성 → hugo 빌드 검증 → 사용자 검수 대기.

## 수익화 로드맵 (맥락)
GitHub Pages 배포(3) → Search Console·네이버 서치어드바이저 등록(4) → 뉴스 주 2~3편 + 가이드 주 3~4편 축적(5) → 글 20~30개 + 플레이스홀더 교체 후 AdSense 신청(6) → 확장(7).
상세 체크리스트는 PROJECT-STATE.md.
